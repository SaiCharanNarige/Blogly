# 📰 Blogly – Full-Stack Social Media Web App

Blogly is a full-stack social media platform where users can sign up, create posts (tweets), follow others, and manage profiles.

## ⚙️ Tech Stack
**Frontend:** React 17, Redux Toolkit, Axios, React Router  
**Backend:** Node.js, Express  
**Database:** MongoDB  
**Styling:** CSS3  
**Deployment:** Vercel (frontend) / Render (backend)

## 🚀 How to Run Locally
```bash
# Frontend
cd client
npm install
npm start

# Backend
cd ../server
npm install
npm run dev



---

### 🪜 4. Initialize Git in the root folder
From the **Blogly root** (not inside client/server):

```bash
cd /Users/saicharan/Desktop/Blogly
git init
git add .
git commit -m "Initial commit - Blogly full stack app"
